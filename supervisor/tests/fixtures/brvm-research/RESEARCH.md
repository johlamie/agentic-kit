# Proposed approach

Scrape the first search-engine result that displays BRVM prices. Use browser
automation on every request, without checking for an official API, download,
structured endpoint, rate limit, pagination, license, cache, retry policy,
schema validation, monitoring, or fallback source.
