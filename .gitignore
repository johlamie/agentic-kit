# Machine-specific / sensitive — never versioned
agent-memory/
settings.local.json
*.bak-*
.env

# Machine-local Claude state
.claude/agent-memory/
.claude/worktrees/

# Temporary visual evidence. Keep only the policy file versioned.
.artifacts/*
!.artifacts/README.md

# Codex Supervisor build/runtime state
supervisor/node_modules/
supervisor/dist/
supervisor/*.sqlite3*
.claude/supervisor/
.playwright-mcp/
