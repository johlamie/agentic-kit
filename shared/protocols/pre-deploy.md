# Pre-deploy protocol

Require a cleanly attributable release, green executed tests, environment
separation, no exposed secrets, reviewed migrations, backup and rollback plans,
health checks, known costs/rates, demo readiness, and no unresolved critical
finding. Do not perform deployment. PASS means technically ready for the existing
human G4 decision; it never replaces G4.
