# Predeclared criteria — codex / qa

Before execution: a fresh synthetic Git project must initialize; two codex/qa checkpoints must persist with attribution and next action; separate log processes must print exactly the same sorted event contents; irrelevant extensions must be ignored. Event-file, events-directory and .agentic-directory symlinks to external synthetic canaries must return a clear refusal without exposing the canary. A FIFO named as an event must fail within the three-second subprocess bound. Removing hostile fixtures must restore the original log. Candidate source and synthetic external canary must remain unchanged.

No true model launch, personal memory, provider action or global configuration change is in scope.
